namespace AISystem.Civil.Iterators
{
    public enum NODE_ITERATOR
    {
        IN_ORDER,
        UNTIL_REQUIREMENT_MET,
        RANDOM_ORDER,
        RANDOM_UNIQUE_ORDER,
        UNTIL_FAIL,
        WEIGHTING_BASED,
        UNTIL_REQUIREMENT_WEIGHTING_BASED
    }
}
